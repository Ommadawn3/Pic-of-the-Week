export default function Loading() {
  return (
    <main className="mx-auto flex min-h-full w-full max-w-md flex-1 items-center justify-center">
      <p className="animate-pulse font-marker text-2xl text-muted">Loading…</p>
    </main>
  );
}
